{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "Desktop-only capability for the main window. Holds the updater permission because the updater plugin ships with macOS/Windows/Linux-only IPC commands (it has no commands on mobile). window-state is server-side only and needs no IPC permission.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default"]
}
