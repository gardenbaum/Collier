{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "quick-pane-capability",
  "description": "Capability for the quick pane floating window. Least-privilege: only what the quick-pane webview itself invokes; show/hide/focus are driven from Rust via custom commands.",
  "windows": ["quick-pane"],
  "permissions": ["core:default", "core:event:default", "core:event:allow-emit"]
}
