# Dependencies
node_modules/

# Build outputs
dist/
build/

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# Config files that should keep their formatting
*.config.js
*.config.ts
vite.config.ts

# Generated files
package-lock.json
.git/
src/lib/bindings.ts

# Logs
*.log

# jscpd output (machine-generated JSON; prettier --check would
# otherwise fail on historical .bak snapshots from pre-dedup runs)
jscpd-report/
jscpd-report.bak/
# beads CLI runtime state (per-machine, gitignored at .beads/.gitignore:17;
# prettier --check otherwise fails on the missing trailing newline the bd
# tool emits when it rewrites the file)
.beads/push-state.json

# Custom dev Dockerfiles (no prettier parser for them)
Dockerfile.dev
Dockerfile.dev.*

# Renovate config files (JSON5 with trailing-comments; no built-in
# prettier parser for .json5 without the prettier-plugin-json5 plugin,
# and renovate config is read-only declarative policy anyway)
renovate.json
renovate.json5
.renovaterc
.renovaterc.json
.renovaterc.json5
